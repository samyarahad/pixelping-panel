# آدرس دیتابیس — روی Railway با Volume در مسیر /data
DATABASE_URL=file:/data/x4g.db

# کلید امضای کوکی نشست — یک رشته تصادفی طولانی بگذارید
SESSION_SECRET=x4g-change-this-to-a-long-random-string

# ادمین پیش‌فرض (فقط بار اول ساخته می‌شود)
DEFAULT_ADMIN_USER=admin
DEFAULT_ADMIN_PASS=admin123
