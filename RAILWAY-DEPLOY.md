# 🚄 راهنمای دیپلوی PIXEL PING روی Railway

این پنل ۱۰۰٪ آماده دیپلوی روی [Railway](https://railway.app) است. همه فایل‌های لازم (`Dockerfile` و `railway.json`) در ریشه پروژه قرار دارند.

---

## ✨ مهم: پنل خودش نود است!

برخلاف پنل‌های معمولی، **PIXEL PING یک سرور VLESS داخلی دارد**:

```
کاربر (v2rayNG / Hiddify / Streisand)
        │  VLESS over WebSocket + TLS
        ▼
دامنه Railway پنل  ──►  سرور لبه PIXEL PING
                          ├── پنل مدیریت (همین سایت)
                          └── نود VLESS داخلی
                                ├── اعتبارسنجی UUID از دیتابیس
                                ├── چک انقضا و سهمیه هر کانکشن
                                └── شمارش ترافیک واقعی → ثبت در دیتابیس
        │
        ▼
   اینترنت (از خود Railway)
```

یعنی **بدون VPS اضافه**، هر کاربری که در پنل می‌سازید لینک اشتراک می‌گیرد و مستقیم از طریق دامنه Railway پنل وصل می‌شود. ترافیکش هم واقعی شمارش می‌شود و در داشبورد می‌بینید.

> اگر بعداً خواستید ترافیک از سرور دیگری رد شود، در **تنظیمات پنل → سرور نود** آدرس یک VPS خارجی را وارد کنید.

---

## مراحل دیپلوی

### گام ۱: ریپازیتوری گیت‌هاب
این پروژه از قبل در ریپازیتوری شماست:
`https://github.com/visavisa4you-hue/pixel-and-ping`

### گام ۲: ساخت پروژه در Railway
1. وارد [railway.app](https://railway.app) شوید (با گیت‌هاب لاگین کنید)
2. **New Project** → **Deploy from GitHub repo**
3. ریپازیتوری `pixel-and-ping` را انتخاب کنید
4. Railway به صورت خودکار `Dockerfile` را شناسایی و بیلد می‌کند

### گام ۳: افزودن Volume (اختیاری ولی توصیه‌شده)
1. روی سرویس راست‌کلیک/منو → **Attach Volume**
2. Mount path را بگذارید: `/data`
3. دیتابیس SQLite پنل در این مسیر ذخیره می‌شود و با هر دیپلوی از بین نمی‌رود

> بدون Volume هم پنل بالا می‌آید (دیتابیس محلی می‌شود) ولی با هر دیپلوی داده‌ها پاک می‌شوند.

### گام ۴: متغیرهای محیطی — هیچ چیزی لازم نیست! ✨
پنل **صفر-کانفیگ** است:
- `SESSION_SECRET` → خودکار ساخته و در دیتابیس ذخیره می‌شود
- رمز ادمین اولیه → خودکار یک رمز قوی تصادفی ساخته و در **لاگ‌های استارت** چاپ می‌شود (بعد از اولین ورود، پنل خودش تغییر رمز را اجباری می‌کند)
- مسیر دیتابیس → خودکار `/data` پیدا می‌شود

اگر خواستید دستی تنظیم کنید (همه اختیاری‌اند):

| متغیر | پیش‌فرض | توضیح |
|-------|---------|-------|
| `DATABASE_URL` | `file:/data/pixelping.db` | مسیر دیتابیس SQLite |
| `DEFAULT_ADMIN_USER` | `admin` | نام کاربری ادمین اولیه |
| `DEFAULT_ADMIN_PASS` | تصادفی (در لاگ) | رمز ادمین اولیه — فقط قبل از اولین ورود اثر دارد |
| `SESSION_SECRET` | تصادفی (در دیتابیس) | کلید امضای نشست |
| `DEMO_DATA` | `1` | با `0` کاربران نمونه ساخته نمی‌شوند |

### گام ۵: دامنه عمومی
1. تب **Settings** → **Networking** → **Generate Domain** (پورت 3000)
2. Railway آدرسی مثل `pixelping-panel-production.up.railway.app` می‌دهد
3. پنل + نود VLESS هر دو روی همین دامنه بالا می‌آیند 🎉

### گام ۶: اولین ورود و ساخت کاربر
1. آدرس پنل را باز کنید → تب **Deployments/Logs** را باز کنید و خط `INITIAL ADMIN PASSWORD` را پیدا کنید
2. با نام کاربری `admin` و آن رمز وارد شوید
3. پنل بلافاصله صفحه **تغییر اجباری رمز** را نشان می‌دهد — رمز قوی خودتان را بگذارید
4. در تب **کاربران** یک کاربر بسازید (حجم + مدت اعتبار)
5. روی **جزئیات** بزنید → لینک اشتراک یا QR را در کلاینت وارد کنید
6. وصل شوید — ترافیک واقعی در داشبورد نمایش داده می‌شود ✅

---

## نکته‌های مهم

- **پروتکل‌ها:** VLESS (اصلی، از نود داخلی)، به‌علاوه Vmess/Trojan/Shadowsocks که برای اتصال به نود خارجی تولید می‌شوند. روی Railway فقط **WebSocket** کار می‌کند که VLESS+WS پشتیبانی کامل دارد.
- **Cloudflare:** می‌توانید دامنه Railway را پشت Cloudflare ببرید (پروکسی نارنجی ابری) — چون ترافیک WS است، سازگار است.
- **آپدیت:** هر push به گیت‌هاب = بیلد و دیپلوی خودکار. دیتابیس روی Volume امن می‌ماند.
- **حالت دمو:** در تنظیمات می‌توانید حالت نمایشی را خاموش کنید تا فقط ترافیک واقعی نمایش داده شود.

## رفع اشکال

| مشکل | راه‌حل |
|------|--------|
| بیلد fail شد | لاگ تب Deployments را ببینید؛ `bun.lock` باید در ریپو باشد |
| پنل بالا نمی‌آید | `DATABASE_URL` و Volume `/data` را چک کنید |
| کاربر وصل نمی‌شود | دامنه Railway باید HTTPS باشد (پیش‌فرض هست)؛ مسیر WS باید `/pixelws` باشد |
| ترافیک ثبت نمی‌شود | چند ثانیه صبر کنید (هر ۱۰ ثانیه یک‌بار ثبت می‌شود) |
| healthcheck fail | `railway.json` باید healthcheckPath `/api/health` داشته باشد (ست است) |

## اجرای محلی با Docker

```bash
docker build -t pixel-ping .
docker run -p 3000:3000 -v pixel-data:/data pixel-ping
# http://localhost:3000
```
